Current issues:

Record-type CUIs are blank

Names are generated correctly.
Cluster matching returns no usable document members or BigQuery rows.
Result preserves names but returns "coding": [].
Document matching is still dependent on exact member-name lookup

Lookup is now case/whitespace-insensitive.
Semantic/alias differences still produce no BigQuery match.
Temporal output falls back to the default

The live model often returns no structured window.
The AIF temporal vocabulary currently builds with zero canonical windows.
The resolver therefore falls back to Less Than One Year.
Tag CUIs are blank

No tag vocabulary is configured for v2/v3.
Tags such as medications have names/topics but no coding.
Record type names are model-dependent

If the LLM emits no record types, the cluster service has nothing to match.
There is no deterministic fallback record-type generator.
Live latency is high

The latest live request took about 84 seconds, mainly from multiple LLM calls and cluster lookups.
